import pytest
from server import create_app
from pathlib import Path

@pytest.fixture
def filesystem():
    #app = create_app()
    # create a directory and file in the current directory 
    cur_dir = Path.cwd()
    sub_dir = Path(cur_dir, 'api_test_dir')
    sub_dir.mkdir()

    file = Path(cur_dir, 'api_test_file')
    return cur_dir

def test_index(filesystem):
    print(filesystem)
